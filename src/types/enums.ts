export enum Color {
  White = 1,
  Black = 2,
}

export enum Piece {
  Pawn = 1,
  King = 2,
  Queen = 3,
  Rook = 4,
  Bishop = 5,
  Knight = 6,
}

export enum BoardState {
  Idle = 1,
  Selected = 2,
}
